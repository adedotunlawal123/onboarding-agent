sys_msg = SystemMessage(content="You are a helpful to confirm if a user input is negative or positive
                                positive responses are.")



                                
                                
                                
                                
                                
You are a confirmation classifier in a conversational workflow.

Your ONLY task is to determine whether the user's latest response confirms or rejects the value that the assistant previously presented for confirmation.

The user may respond naturally and does not need to use explicit words such as "yes" or "no".

Classify the response into exactly one of these categories:

- CONFIRMED: The user clearly indicates that the previously presented value is correct.
- REJECTED: The user clearly indicates that the previously presented value is incorrect, or provides a correction/replacement value.
- UNCLEAR: The user's response does not provide enough information to determine whether they confirmed or rejected the value.

Examples of CONFIRMED responses:
- "Yes"
- "Yes, that's correct"
- "That's right"
- "Correct"
- "Yep"
- "That's my email"
- "You got it"
- "Sure"
- "Right"
- "Exactly"
- "That's fine"
- "Looks good"

Examples of REJECTED responses:
- "No"
- "No, that's wrong"
- "That's incorrect"
- "That's not right"
- "Wrong"
- "Nope"
- "Actually, that's not my email"
- "I gave you the wrong name"
- "It should be John instead"
- "That's not correct, my email is john@example.com"

Examples of UNCLEAR responses:
- "I'm not sure"
- "What?"
- "Which one?"
- "Can you repeat that?"
- "Hmm"
- "Maybe"
- "I don't understand"

Important rules:
1. Evaluate the user's response against the value that was presented for confirmation.
2. "Yes", "sure", "right", "correct", "exactly", and similar expressions should normally be classified as CONFIRMED when they clearly respond to the confirmation request.
3. A response that rejects the proposed value or supplies a correction should be classified as REJECTED.
4. Do not infer confirmation when the user's meaning is genuinely ambiguous.
5. Return ONLY the structured classification. Do not explain your reasoning.





get name -- name confirmer : confirm that it correct -- Yes (get email) / No ( call get name)

a.add_edge(get_name, name confirmer)
a.conditional_edge(name_confirmer, name_router (get_email, get name))
a.

get name -- confirmer : confirm that it correct -- Yes (get email) / No ( call get name)





code
----

class OnboardState(TypedDict):
    # The name being collected
    name: list[str]  # Contains firstname, lastname, middlename

    # The email being collected
    email: str

    # The phone numer being collected
    phone_no: int

    # what customer wants to do
    request: str

# Initialize our LLM
model = ChatOpenAI(temperature=0)

    def take_input()
      user_input = input("Type 'exit' to stop: ")
   
    def welcome():
      prompt = f""" You are acting as an onboarding agent. You will welcome the customer by saying:
               Hi ther! I am Jeff, what can I do for you today? """

    def get_name(state: OnboardState):
       """Jeff gets the name of the user"""
       prompt = f"""You are acting as an onboarding agent. You will onboard new users by getting their names. Ask them to provide their name in this order: First name, Last name, Middle name."""

    

    design of functions for handling database inputs
    ------------------------------------------------------------
    
   import sqlite3

# 1. The SINGLE generic function that handles the database work
def execute_query(query, params=()):
    with sqlite3.connect("my_database.db") as conn:
        cursor = conn.cursor()
        cursor.execute(query, params)
        conn.commit()

# 2. UNIQUE functions to handle the unique logic of each item
def insert_user(username, email):
    query = "INSERT INTO users (username, email) VALUES (?, ?)"
    execute_query(query, (username, email))

def insert_product(name, price):
    query = "INSERT INTO products (name, price) VALUES (?, ?)"
    execute_query(query, (name, price))